apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: petclinic-dev

resources:
  - resource-quota.yaml

# TODO: E-16 replaces this overlay with Helm chart deployment
# This overlay is transitional and defines the namespace-level resources (quotas)
# that are not managed by the per-service Helm chart.
